<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.didk.dao.ChatFriendRequestDao">

    <resultMap type="com.didk.entity.ChatFriendRequestEntity" id="friendRequestMap">
        <result property="id" column="id"/>
        <result property="fromUserId" column="from_user_id"/>
        <result property="toUserId" column="to_user_id"/>
        <result property="status" column="status"/>
        <result property="verifyMessage" column="verify_message"/>
        <result property="createDate" column="create_date"/>
        <result property="processDate" column="process_date"/>
    </resultMap>

    <select id="selectByUserId" resultType="com.didk.entity.ChatFriendRequestEntity" parameterType="long">
        SELECT * FROM chat_friend_request
        WHERE
        (from_user_id = #{userId} OR to_user_id = #{userId})

        <if test="lastCreateDate != null">
            AND create_date &lt; #{lastCreateDate}
        </if>

        ORDER BY create_date DESC
        LIMIT #{limit}
    </select>

    <select id="selectByFromAndToUser" resultType="com.didk.entity.ChatFriendRequestEntity">
        select * from chat_friend_request where from_user_id = #{fromUserId} and to_user_id = #{toUserId}
    </select>

</mapper>