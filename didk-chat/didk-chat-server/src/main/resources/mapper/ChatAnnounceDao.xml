<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.didk.dao.ChatAnnounceDao">

    <resultMap type="com.didk.entity.ChatAnnounceEntity" id="announceMap">
        <result property="id" column="id"/>
        <result property="roomId" column="room_id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="imageUrl" column="image_url"/>
        <result property="isPinned" column="is_pinned"/>
        <result property="createDate" column="create_date"/>
    </resultMap>

    <select id="selectByRoomId" resultType="com.didk.entity.ChatAnnounceEntity">
        select chat_announce.id,
               room_id,
               title,
               content,
               image_url,
               is_pinned,
               create_date,
               sys_user.username
        from chat_announce
                 join sys_user on chat_announce.user_id = sys_user.id
        where room_id = #{roomId}
    </select>

    <select id="selectByUserIdAndRoomId" resultType="com.didk.entity.ChatAnnounceEntity" parameterType="long">
        select chat_announce.id,
               room_id,
               title,
               content,
               image_url,
               is_pinned,
               create_date,
               sys_user.username
        from chat_announce
                 join sys_user on chat_announce.user_id = sys_user.id
        where room_id = #{roomId}
          and user_id = #{userId}
    </select>

    <select id="selectPinnedByRoomId" resultType="com.didk.entity.ChatAnnounceEntity" parameterType="long">
        select *
        from chat_announce
        where room_id = #{roomId}
          and is_pinned = 1
    </select>

    <select id="getById" resultType="com.didk.entity.ChatAnnounceEntity" parameterType="java.lang.Long">
        select chat_announce.id,
               room_id,
               user_id,
               title,
               content,
               image_url,
               is_pinned,
               create_date,
               sys_user.username,
               head_image
        from chat_announce
                 join sys_user on chat_announce.user_id = sys_user.id
        where chat_announce.id = #{id}
    </select>

</mapper>